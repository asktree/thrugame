{
  "name": "great-work-client",
  "version": "0.1.0",
  "private": true,
  "description": "GREAT WORK! — Thru client: seal solutions on-chain and read the leaderboard",
  "type": "module",
  "scripts": {
    "bundle": "esbuild browser.js --bundle --format=iife --minify --target=es2020 --outfile=../demo/gw-chain.js",
    "submit": "node submit.js",
    "leaderboard": "node leaderboard.js"
  },
  "dependencies": {
    "@thru/passkey": "^0.3.11",
    "@thru/programs": "^0.3.11",
    "@thru/sdk": "^0.3.11",
    "@thru/wallet": "^0.3.11"
  },
  "devDependencies": {
    "esbuild": "^0.25.4"
  }
}
